<header class="bg-white shadow-md fixed top-0 left-0 right-0 z-40">
    <div class="container mx-auto px-4 py-3">
        <div class="flex justify-between items-center">
            <!-- Logo and Brand -->
            <div class="flex items-center space-x-3">
                <button id="mobileMenuButton" class="md:hidden bg-blue-600 text-white p-2 rounded-lg mr-2">
                    <i class="fas fa-bars text-lg"></i>
                </button>
                <div class="bg-blue-600 p-2 rounded-lg">
                    <i class="fas fa-solar-panel text-white text-xl"></i>
                </div>
                <div>
                    <h1 class="text-xl font-bold text-gray-800">Solar Monitoring System</h1>
                    <p class="text-sm text-gray-600">Kadidi Health Center, Lunzu</p>
                </div>
            </div>
            
            <!-- Right side: User info -->
            <div class="flex items-center space-x-4">
                <!-- User Menu -->
                <div class="relative">
                    <button id="userMenuButton" class="flex items-center space-x-2 focus:outline-none">
                        <div class="w-10 h-10 bg-blue-100 rounded-full flex items-center justify-center">
                            <i class="fas fa-user text-blue-600"></i>
                        </div>
                        <div class="hidden md:block text-left">
                            <p class="text-sm font-medium text-gray-800"><%= user.name %></p>
                            <p class="text-xs text-gray-600 capitalize"><%= user.role %></p>
                        </div>
                        <i class="fas fa-chevron-down text-gray-400"></i>
                    </button>
                </div>
            </div>
        </div>
    </div>
</header>